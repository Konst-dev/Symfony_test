{% extends 'base.html.twig' %}

{% block body %}
   <h1> {{message}}</h1>
<script>
	let books={};
    async function sendData(url, data) {
        fetch(url, {
                method: 'GET',
                headers: {
                    'Content-Type': 'application/json',
                },
                //body: JSON.stringify(data),
            })
            .then(response => response.json())
            .then((data) => {
                //if (data['sent'] === 'OK') {
                    print(data);
                    console.log(data);
                    //alert(data['message']);
                   // window.location.href = '/';
               // } else
               //     console.log('Ошибка отправки данных');
            })
            .catch((error) => {
                console.error(error);
            });
    }
	
		sendData('/books/getallbooks',[]);
        function print(data){
            console.log(data);
            for(var el in data){
                console.log(el);
                document.write('<p>'+data[el]['title']+', '+data[el]['title']+'</p>');
            }
        }

	</script>
{% endblock %}